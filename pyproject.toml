[tool.poetry]
name = "tpu-compute"
version = "0.1.0"
description = ""
authors = ["bramiozo <bramiozo@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
transformers = "^4.47.0"
accelerate = "^1.2.0"
tokenizers = "^0.21.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
